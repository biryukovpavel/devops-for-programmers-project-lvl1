version: '2.4'

services:
  app:
    build:
      context: .
      dockerfile: ./Dockerfile.production
    image: biryukovpyu/devops-for-programmers-project-lvl1
    command: ["npm", "test"]
    mem_limit: 768M
